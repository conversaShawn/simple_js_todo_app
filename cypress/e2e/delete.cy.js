describe('deletes a new todo', () => {
    beforeEach('visit baseUrl', () => {
      // visit baseUrl
    })
    it('deletes with click', () => {
      // verifies length of list (3)
      
      // verify length of list(4)
    })
    it('delete with form', () => {
      // verify length of list (3)
      
      // verify length of list (4)
    })
    it('deletes all todos', () => {
      // verify length of list (3)
      
      // verify length of list (4)
    })
  })