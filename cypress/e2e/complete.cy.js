describe("checks if todo is complete", () => {
  beforeEach("visit baseUrl", () => {
    // visit baseUrl
  });
  it("checks text css", () => {
    // 
  });
  it("is checkbox checked", () => {
    // 
  });
  it("adds new todo and checks css", () => {
    // 
  });
});
